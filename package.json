{
  "name": "foodstorereact",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "prebuild": "cd frontend && npm install & cd backend && npm install",
    "build": "cd frontend && npm run build",
    "postbuild": "cp -r frontend/dist backend/src/public && rm -r frontend/dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "description": "",
  "engines": {
    "node": ">=18 <19"
  }
}
